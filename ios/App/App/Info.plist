<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>Forge</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<!-- iPhone: portrait-only. Landscape was briefly supported so the
	     video-analysis screen could be viewed rotated, but iOS's own
	     rotation transition between the two proved unreliable on-device (a
	     stuck-sideways dialog with no way to rotate back, and a squished
	     layout right after starting playback) -- every screen in this app
	     is designed for portrait, and none of it depends on landscape
	     actually working, so removing it outright is a real fix, not a
	     workaround. iPad keeps all four orientations: App Store validation
	     rejects a bundle that restricts iPad's orientations without also
	     setting UIRequiresFullScreen to opt out of Split View/Slide Over
	     multitasking (error 90474, which the first attempt at this fix hit),
	     and nothing about the reported bug involved iPad in the first
	     place -- every report has been on iPhone. -->
	<!-- Landscape is advertised here ONLY so iOS will consider rotating for the sprint
	     tracker. It does not mean the app is free to rotate: AppDelegate's
	     supportedInterfaceOrientationsFor returns .portrait for every screen except while
	     AvBodyTrackingPlugin has a landscape capture open, and that callback overrides this
	     list. Removing either half breaks the sprint camera or unlocks rotation everywhere. -->
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
	<key>NSCameraUsageDescription</key>
	<string>Forge uses your camera for live rep/sprint/mechanics tracking, form-check video recording, and food/barcode scanning.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Forge lets you attach photos from your library for food logging and lesson content uploads.</string>
	<key>NSPhotoLibraryAddUsageDescription</key>
	<string>Forge can save form-check videos and shareable PR cards to your Photos library when you choose to download them.</string>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>NSFaceIDUsageDescription</key>
	<string>Forge uses Face ID to unlock the app if you enable the optional lock screen in Notification Settings.</string>
	<!-- Must name every type client/src/lib/native-health.ts actually asks
	     for. This string is what the athlete reads on the permission
	     sheet, and it named three of the eight. See the Info.plist test in
	     client/src/lib/health-permissions.test.ts, which fails if the two
	     drift apart again.
	     NSHealthUpdateUsageDescription has to stay even though the app never
	     writes to Health. It was removed once, on the reasoning that
	     Health.requestAuthorization is called with write: [] so HealthKit
	     never asks for write access at runtime. That reasoning was about the
	     wrong thing: Apple checks the compiled BINARY, not what it calls,
	     and the bundled Capacitor health plugin references HealthKit's write
	     APIs whether or not this app passes anything to them. App Store
	     Connect rejected the upload with error 90683, and its own wording
	     covers exactly this case -- "If you're using external libraries or
	     SDKs, they may reference APIs that require a purpose string. While
	     your app might not use these APIs, a purpose string is still
	     required."
	     Note the verify_build lane does NOT catch this: it archives and
	     signs, and 90683 comes from altool during upload, so a build can be
	     green and still be rejected on the way to TestFlight.
	     The string says plainly that nothing is written, since that is the
	     honest answer to "why does this app need to write my health data"
	     and the permission is never actually requested. -->
	<key>NSHealthUpdateUsageDescription</key>
	<string>Forge never writes anything to Apple Health and never asks for permission to. This appears only because a component the app is built with references Health's write functions; Forge reads the metrics described below and writes nothing back.</string>
	<key>NSHealthShareUsageDescription</key>
	<string>Forge reads your sleep, resting heart rate, heart rate variability, VO2 max, respiratory rate, and weight from Health to pre-fill your daily check-in, plus your heart rate and workouts around a session to work out your heart rate recovery. Only if you turn on Health sync in Notification Settings.</string>
</dict>
</plist>
